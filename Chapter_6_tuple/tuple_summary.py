# tules
# tules are immutable
# tules are ordered collection of data
# tules can store any data type 
# you cannot change(add or delete) values from tules once it created
# but can add, delete data from list which is present inside tuples

mixed = (1,2,3,4,5,6,7,'eight')
# no append, no pop, no insert, no remove

# methods 
# count and index
# len fuction
# slicing
example = ('one', 'two', 'three')
print(example[:2])

# functions
# min(), max(), len(), sum()

mixed2 = (1,2,3,4,5,[6,7,8,])
mixed2[5].pop() 
print(mixed2)